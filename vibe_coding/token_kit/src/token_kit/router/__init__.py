"""RESERVED for the matrix-routing lane: the call cap and agent_trigger_matrix.toml routing.

Implement `handle(payload) -> int` here; cli.py's `hook` subcommand imports it
and, while this package is empty, exits 0 (the hook has no opinion).
"""
